doctype html
html(lang="en")
  include ../common/head.pug
  body
    include ../common/menubar.pug
    main(class="container-sm")
      h1 404
      h2 Site not found
      p The page you were looking for (#{requestedPage}) does not exist
    include ../common/bootstrap.pug