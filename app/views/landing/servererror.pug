doctype html
html(lang="en")
  include ../common/head.pug
  body
    include ../common/menubar.pug
    main(class="container-sm")
      h1 500
      h2 Internal Server Error
      p Something went wrong internally. Here's the error message:
      p #{error}
      p #{stack}
    include ../common/bootstrap.pug